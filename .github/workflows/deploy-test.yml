name: Deploy Worklog Test

on:
  push:
    branches: ["main"]

jobs:
  deploy-test:
    runs-on: self-hosted
    steps:
      - name: Run full deployment pipeline
        shell: bash
        run: |
          /home/simeon/deploy_worklog.sh